# SC0479 小程序 V2.0 自检报告

## 已完成检查

1. 所有 JavaScript 文件通过 `node --check` 语法检查。
2. 所有 Vue 页面 `<script setup>` 通过 JavaScript 语法检查。
3. Vue 文件的 template / script / style 基础结构检查通过。
4. `@/` 别名导入路径完整性检查通过。
5. 使用 Node 模拟运行环境执行以下状态测试：
   - 空设备列表加载；
   - 新设备绑定与切换；
   - `status_report` 三路状态解析；
   - 手动优先状态解析；
   - 任务状态缓存；
   - MQTT pending 注册；
   - `dev_id + seq + ack_cmd` 模拟 ACK 匹配；
   - ACK 后 pending 清除；
   - 多设备订阅 Topic 数量检查。
6. 运行结果：`SC0479 V2.0 runtime tests: PASS`。
7. 工程中已移除 V1.4.2 正式业务引用：
   - ds3；
   - get_status；
   - time_sync；
   - set_net_rule / get_net_rule；
   - 远程软重启；
   - 旧控制页和旧计划页路由。

## 环境限制

当前执行环境没有 HBuilderX 和微信开发者工具，因此无法代替目标开发环境完成：

- uni-app 实际编译；
- 微信开发者工具代码质量扫描；
- 真机扫码；
- 正式 MQTT Broker 和 MCU 联调。

工程已尽可能完成静态检查和核心状态机模拟测试。首次打开后建议依照《测试清单.md》完成一次编译和真机联调。


## UI修正027静态检查

- 已检查9位数字自动补前缀逻辑。
- 已检查旧 `GW4G_FACTORY` 缓存过滤逻辑。
- 已检查稳定添加顺序 `order` 的保存和恢复。
- 已检查在线/离线设备分组及排序。
- 已检查离线设备简洁行模板。
- 首页、设置页和日志页脚本均通过 `node --check`。
