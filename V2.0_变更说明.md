# SC0479 小程序 V2.0 变更说明

## 通道

正式通道由四路调整为三路：

- `ds1`：网口 1
- `ds2`：网口 2
- `dc12v`：光猫

已删除网口 3 正式界面和旧协议调用。

## 控制语义

- 网口 `enabled=true`：网络连接。
- 网口 `enabled=false`：网络断开。
- 光猫 `enabled=true`：光猫开启。
- 光猫 `enabled=false`：光猫关闭。

页面不直接显示 MCU 原始 IO 电平。

## 定时任务

- 每通道仅一个任务。
- 每任务最多 3 个时间段。
- 时间精度 1 分钟。
- 不允许跨天。
- 不允许时间段重叠。
- 一个任务不能控制多个通道。

## 间歇断网

- 每台设备一套全局参数。
- 仅网口 1、网口 2 参与。
- 光猫不参与。
- 两个网口独立随机执行。
- 仅在对应网口任务的有效时间段内执行。

## 手动优先

- 手动控制后对应通道进入手动优先。
- 定时任务和间歇断网同时暂停。
- 直到用户主动恢复自动。
- 支持单通道恢复和全部恢复。

## 删除功能

V2.0 不提供：

- 远程软重启
- 首页主动刷新命令
- 小程序校时
- RTC 同步状态显示
- 上位机滚码和 4G 诊断功能

滚码和 4G 诊断属于 UART0 上位机程序，与本小程序完全分离。


## UI与设备绑定修正 027

- 首次运行默认无设备，自动移除旧缓存中的 `GW4G_FACTORY`。
- 手动添加只输入9位数字，系统自动生成 `GW4G_` 前缀。
- 扫码以9位数字为主，自动生成完整设备ID。
- 设备按添加顺序保存稳定编号，从1开始。
- 首页在线设备置顶。
- 离线设备不再显示完整控制卡片，仅显示简洁离线状态行。
- 设备标题去掉“设备”文字，改用设备图标和顺序编号。
- 当前 `wss://broker.emqx.io:8084/mqtt` 仍为联调地址，正式服务器及变更方式待项目确认。


## 设备卡片显示修正 028

- 撤销027中“离线设备只显示状态行”的改动。
- 在线、离线设备均恢复为完整设备卡片。
- 在线设备继续置顶。
- 离线设备卡片降低亮度，控制入口保持禁用。
- 删除标题中的“设备”文字。
- 删除设备1、设备2、设备3等顺序编号显示。
- 设备图标后直接显示完整设备ID，例如 `GW4G_000000001`。
- 保留027中的9位数字自动补 `GW4G_`、扫码绑定和默认无设备逻辑。
